#ifndef ENUM_INPUT_HPP
#define ENUM_INPUT_HPP
#include <GLFW/glfw3.h>
#include <GLFW/glfw3.h>

enum class Input {
	right,
	left,
	up,
	down,
	forward,
	backward,
    leftClick,
    rightClick,
	escape,
	count
};

#endif